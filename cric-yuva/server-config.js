// Cric Yuva deployment configuration.
// When the app and Node server are hosted together, keep this empty.
// If frontend is hosted separately, set this to the public API server origin.
window.CRIC_YUVA_API_BASE = "";
